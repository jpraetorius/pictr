<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @options[:title] %></title>
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le styles -->
    <link href="css/main.css" rel="stylesheet">

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">
  </head>

  <body>

    <div class="topbar">
      <div class="fill">
        <div class="container">&nbsp;</div>
      </div>
    </div>

    <div class="container">
      <h1 class="below"><%= @options[:title]%><small> | <%= @options[:total_number_of_pictures] %> Bilder</small></h1>
	  <hr />
		<% @images.each_slice(4) do |images|%>
	  <div class="row">
			<% images.each do |image| %>
				<div class="span4"><a href="<%= image.image_html_name%>"><img src="<%= 'tn/'+ image.thumbnail_name%>" alt="<%= h image.caption%>" width="<%= @options[:tn_width]%>" height="<%= @options[:tn_height]%>" title="<%= h image.caption%>" /></a></div>
			<% end %>
    </div>
		<% end %>
	  <div class="pagination">
    	<ul>
    	  <li class="prev <%= first_page?() ? "disabled" : "" %>"><a href="<%= "%04d"%prev_page()+'.html'%>">&larr; Zurück</a></li>
				<% for i in 1..@total_number_of_pages %>
					<% if i == @page_num %>
      			<li class="active"><a href="#"><%= i%></a></li>
					<% else %>
    	  		<li><a href="<%= "%04d"%i+'.html'%>"><%= i%></a></li>
					<% end %>
				<% end %>
    	  <li class="next <%= last_page?() ? "disabled" : "" %>"><a href="<%= "%04d"%next_page()+'.html'%>">Vor &rarr;</a></li>
    	</ul>
      </div>	
	  	  
      <footer>
        <p><span class="muted"> created with <a href="#">pictr</a> on <%= Time.now.strftime('%d. %B %Y')%></span> | &copy; all photos <%=@options[:author]%></p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
