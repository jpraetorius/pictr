<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @options[:title] %></title>
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="generator" content="">

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le styles -->
    <link href="css/main.css" rel="stylesheet">

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">
  </head>

  <body>

    <div class="topbar">
      <div class="fill">
        <div class="container">&nbsp;</div>
      </div>
    </div>

    <div class="container">
      <h1 class="below"><%= @options[:title]%><small> | <%= @options[:total_number_of_pictures] %> Bilder</small></h1>
	  <p><small><a href="<%= @page_html_name %>">&larr; Zurück zur Übersicht aller Bilder der Galerie</a></small><p>
	  <hr />
	  <div class="row">
		<div class="span16">
		<figure>
  			<img src="<%= @image_name %>" alt="<%=h @caption %>" title="<%=h @caption %>">
  			<figcaption class="caption" title="<%=h @tech_details %>"><%=h @long_caption %></figcaption>
		</figure>
		</div>
      </div>
	  <div class="pagination">
    	<ul>
				<% if first_image? %>
    	  <li class="prev disabled"><a href="#">&larr; Voriges Bild</a></li>
				<% else %>
    	  <li class="prev"><a href="<%=@prev_img%>">&larr; Voriges Bild</a></li>
				<% end %>
    	  <li><a href="<%= @page_html_name %>">Galerie-Übersicht</a></li>
				<% if last_image? %>
    	  <li class="next disabled"><a href="#">Nächstes Bild &rarr;</a></li>
				<% else %>
    	  <li class="next"><a href="<%=@next_img%>">Nächstes Bild &rarr;</a></li>
				<% end %>
    	</ul>
      </div>	

      <footer>
        <p><span class="muted"> created with <a href="#">pictr</a> on <%= Time.now.strftime('%d. %B %Y')%></span> | &copy; all photos <%=@options[:author]%></p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
